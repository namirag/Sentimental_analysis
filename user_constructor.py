class user_const:
    def __init__(self, first_name, last_name, gender, birth_date, sen_email, sen_password):
        self.first_name = first_name,
        self.last_name = last_name,
        self.gender = gender,
        self.birth_date = birth_date,
        self.sen_user_email = sen_email,
        self.sen_user_password = sen_password
